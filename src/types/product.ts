export type Product = {
  id: number;
  title: string;
  description: string;
  price: string;
  discountPercentage: string;
  thumbnail: string;
  images: string;
  category: string;
  rating: string;
};

export type Products = Product[];